{
    "name": "placeprep",
    "version": "1.0.0",
    "description": "PlacePrep - Placement Preparation Portal",
    "scripts": {
        "start": "node server/server.js",
        "build": "cd client && npm install && npx vite build",
        "install:server": "cd server && npm install",
        "install:all": "npm run install:server && npm run build",
        "dev": "cd server && npm run dev"
    }
}